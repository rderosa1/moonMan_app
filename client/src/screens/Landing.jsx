import React from 'react'
import Items from './Items'

const Landing = (props) => (
    <div className='container landing'>
        <Items {...props} />
        <h4>Testing</h4>
    </div>
)
export default Landing